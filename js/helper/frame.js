class Frame{
    constructor(x,y,width,height,offX=0,offY=0)
    {
        this.x=x;
        this.y=y;
        this.width=width;
        this.height=height;
        this.offsetX=offX;
        this.offsetY=offY; 
    }
}